name: deploy to ec2
# triggering the workflow
on:
push:
branch:
  - cicd
  
job:
 deploy:
runs-on: ubuntu-latest
steps:
- name: checkout
uses: action/checkout@v2

-name: run a linux command
run: ls -la
