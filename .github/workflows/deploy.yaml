name: deploy to ec2
# triggering the workflow
on:
push:
branch:
  - cicd

job:
 deploy1:
runs-on: ubuntu-latest
steps:
- name: checkout
uses: action/checkout@v2

-name: run a linux command
run: ls -la


deploy2:
runs-on: ubuntu-latest
steps:
- name: checkout
uses: action/checkout@v2

-name: run a linux command
run: cat index.html



name: deploy to ec2
# triggering the workflow
on:
push:
branch:
  - cicd

job:
 deploy3:
runs-on: ubuntu-latest
steps:
- name: checkout
uses: action/checkout@v2

-name: run a linux command
run: ls -la


deploy4:
runs-on: ubuntu-latest
steps:
- name: checkout
uses: action/checkout@v2

-name: run a linux command
run: cat index.html
