name: deploy to ec2

# triggering the workflow
on:
  push:
    branches:
      - cicd

jobs:
  deploy1:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: run a linux command
        run: ls -la

deploy2:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: run a linux command
        run: cat index.html
